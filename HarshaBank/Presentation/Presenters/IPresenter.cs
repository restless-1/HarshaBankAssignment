namespace HarshaBank.Presentation.Presenters
{
    /// <summary>
    /// Base interface which identifies a class as a presenter.
    /// This interface is intended only for categorization - it defines no contract.
    /// </summary>
    internal interface IPresenter
    {
    }
}
